<%- include('layouts/header') %>

    <%- include('layouts/nav') %>
    <% if (isLoggedin) { %>
        <%- include('admin/nav') %>
    <% } %>
    <main>
        <h2><%= post.title %></h2>
        <img src="<%= post.image %>" alt="<%= post.title %>" style="width: 100%; max-width: 600px; height: auto; display: block; margin: 20px auto;">
        <div class="post-content">
            <%- post.content %>
        </div>
        <p><a href="/">Back to Home</a></p>
    </main>

<%- include('layouts/footer') %>